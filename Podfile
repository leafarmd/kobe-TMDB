# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Kobe-TMDB' do
  use_frameworks!

  # Pods for Kobe-TMDB

  target 'Kobe-TMDBTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
